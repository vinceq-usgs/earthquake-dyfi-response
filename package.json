{
  "name": "earthquake-dyfi-response",
  "version": "2.0.0",
  "description": "Subsystem of DYFI that handles collection of user entries from the web interface.",
  "repository": {
    "type": "git",
    "url": "https://github.com/usgs/earthquake-dyfi-response.git"
  },
  "keywords": [
    "dyfi",
    "python",
    "macroseismic",
    "seismology",
    "earthquake",
    "quake",
    "usgs"
  ],
  "scripts": {
    "build": "docker build -t usgs/earthquake-dyfi-response:latest .",
    "start": "echo 'Starting container on http://localhost:8123/' && echo && docker run --rm -p 8123:80 -v $(pwd)/responses:/data usgs/earthquake-dyfi-response:latest",
    "test": "echo 'Write some tests' && exit 0"
  },
  "license": "CC0-1.0",
  "dependencies": {},
  "devDependencies": {},
  "engines": {
    "node": "8"
  }
}
